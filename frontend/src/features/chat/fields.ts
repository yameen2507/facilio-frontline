/**
 * The field-label vocabulary every card on the intake agent page shares — one
 * module so the label and its one-line hint can't drift apart between cards.
 */

export const FIELD_LABEL = "text-muted-foreground mb-1 block text-xs";

/** The one-line explanation under a field — kept to a clause, not a paragraph. */
export const FIELD_HINT = "text-muted-foreground/70 mt-1 block text-[11px]";
